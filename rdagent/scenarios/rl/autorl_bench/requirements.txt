# AutoRL-Bench 依赖
# conda 环境: cwy-rl (Python 3.10)

# RL 训练（核心）
trl>=0.27.0
accelerate>=1.0.0
datasets>=3.0.0

# 评测
opencompass==0.5.1

# 推理加速（可选，TRL 支持 0.10.2-0.12.0）
vllm>=0.12.0

# 数据处理
numpy>=1.26.0
pandas>=1.5.0

# 模型
torch>=2.0.0
transformers>=4.40.0
huggingface_hub>=0.20.0

# Web 服务
flask
flask-cors

# 日志
loguru
requests
pyyaml
