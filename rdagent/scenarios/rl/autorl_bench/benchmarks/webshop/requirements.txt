# WebShop benchmark dependencies
#
# Prerequisites: Java 11+ (JDK) and faiss-cpu
#   conda install -c conda-forge openjdk=11 faiss-cpu
#
# Installation command:
#   pip install -r benchmarks/webshop/requirements.txt
#   python -m spacy download en_core_web_sm
#
# NOTE: Flask/Werkzeug is fixed to version 2.x in the main requirements.txt

#WebShop PyPI package
webshop

# Data download tool
gdown

# WebShop specific dependencies
gym==0.24.0
beautifulsoup4==4.11.1
cleantext==1.1.4
pyserini==0.17.0
rank_bm25==0.2.2
thefuzz==0.19.0
spacy==3.7.2

# Note: Flask/Werkzeug is fixed to 2.x (Flask 3.x is not compatible with WebShop)
flask==2.2.5
Werkzeug==2.2.3