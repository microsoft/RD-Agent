name: "Claude CLI Agent"
description: "One-shot agent: receives a full prompt and uses Claude Code CLI to complete coding, training, and evaluation submission autonomously"
start: "start.sh"
env_vars:
  CLAUDE_MODEL: "claude-sonnet-4-6"
  CLAUDE_TIMEOUT: "36000"
