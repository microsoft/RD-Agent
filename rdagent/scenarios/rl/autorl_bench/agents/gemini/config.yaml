name: "Gemini CLI Agent"
description: "One-shot agent: receives a full prompt and uses Gemini CLI to complete coding, training, and evaluation submission autonomously"
start: "start.sh"
env_vars:
  GEMINI_MODEL: "gemini-2.5-pro"
  GEMINI_TIMEOUT: "36000"
